// src/components/FunkceTab.jsx
import React from 'react';

// Removed layout props as layout mode removed
function FunkceTab() {
    return (
        <div>
            <h2>Funkce (Functions)</h2>
            {/* Removed layout buttons */}
            <p><em>Functionality buttons (Save/Load Layout, etc.) will go here.</em></p>
        </div>
    );
}

export default FunkceTab; // Ensure default export